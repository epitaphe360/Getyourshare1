[build][build]

builder = "NIXPACKS"builder = "NIXPACKS"

buildCommand = "pip install --upgrade pip && pip install -r requirements.txt"

[deploy]

[deploy]startCommand = "bash start.sh"

startCommand = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120"healthcheckPath = "/health"

healthcheckPath = "/health"restartPolicyType = "ON_FAILURE"


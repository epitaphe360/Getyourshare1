[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -l $PORT"
